<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Message Board</title>
</head>
<body>
    <h1><%= title %></h1>
    <a href="/new">Add new message</a>
    <% messages.forEach ((message, index) => { %>
        <div>
            <div>
                <p><%= message.user %></p>
                <p><%= message.text %></p>
                <p><%= message.added %></p>
            </div>
            <a href="/detail/<%= index %>">Open</a>
        </div> 
    <% }); %>
</body>
</html>